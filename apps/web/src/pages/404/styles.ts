import { styled } from "solid-styled-components";
import { Flex } from "~/components/Common/styles";
import { IFlexProps } from "~/components/Common/types";

export const NoFoundContainer = styled(Flex)<IFlexProps>`
  height: 100%;
`;
